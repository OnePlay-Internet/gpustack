# Copy to .env and edit. Used by docker-compose.shared-infra.yaml.

# ---- Image ----
IMAGE_REGISTRY=docker.io
IMAGE_NAMESPACE=gpustack
GPUSTACK_TAG=latest

# ---- Shared database (remote host) ----
# Scheme: mysql | postgresql
DB_SCHEME=mysql
DB_HOST=172.28.77.202
DB_PORT=3306
DB_NAME=gpustack
DB_USER=gpustack
DB_PASSWORD=gpustack_pass

# ---- Grafana (OPTIONAL) ----
# Leave GRAFANA_URL empty/commented -> builtin Grafana+Prometheus run inside the
# gpustack container (UI at host port 13000). Only MySQL is shared.
# Set GRAFANA_URL to a browser-reachable shared Grafana -> builtin is skipped.
GRAFANA_URL=
GRAFANA_WORKER_DASHBOARD_UID=gpustack-worker
GRAFANA_MODEL_DASHBOARD_UID=gpustack-model

# ---- Admin bootstrap (first init only) ----
BOOTSTRAP_PASSWORD=admin
